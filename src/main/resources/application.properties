# Perfil Ativo
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Configurações do Servidor
server.port=${SERVER_PORT:3000}
server.servlet.context-path=/api/v1

# Configurações de Banco de Dados
spring.datasource.url=jdbc:postgresql://database-1.ch0scwkoskku.us-west-2.rds.amazonaws.com:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=t34421664
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações JPA/Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# Configurações JWT
# application.properties
rephelper.jwt.secret=mcTimUGG+7ItswwECWgljT5an1J9oScaiZODpPuwL2w=
rephelper.jwt.expiration=86400000
rephelper.jwt.issuer=your-issuer
rephelper.jwt.audience=your-audience

# Configurações do Firebase
rephelper.firebase.config.api-key=${FIREBASE_API_KEY}
firebase.config.auth-domain=${FIREBASE_AUTH_DOMAIN}
firebase.config.database-url=${FIREBASE_DATABASE_URL}
firebase.config.project-id=${FIREBASE_PROJECT_ID}
firebase.config.storage-bucket=${FIREBASE_STORAGE_BUCKET}
firebase.config.messaging-sender-id=${FIREBASE_MESSAGING_SENDER_ID}
firebase.config.app-id=${FIREBASE_APP_ID}
#application.properties
cors.allowed-origins=http://localhost:4200,https://your-frontend.com
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=Authorization,Content-Type,X-Requested-With
cors.max-age=3600
# Credenciais do Firebase Admin SDK
firebase.service-account-key=${FIREBASE_SERVICE_ACCOUNT_KEY}

# Configurações de Logging
logging.level.root=INFO
logging.level.com.seu.pacote=DEBUG
logging.level.org.springframework.security=DEBUG

# Configurações OpenAPI/Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# Ambiente de Desenvolvimento
spring.profiles.include=dev
node.env=${NODE_ENV:development}